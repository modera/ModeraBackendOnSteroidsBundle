framework:
    secret:          Hell yeah!
    session:         ~
    templating:
        engines: ['twig']

# Twig Configuration
twig:
    debug:            '%kernel.debug%'
    strict_variables: '%kernel.debug%'

modera_backend_on_steroids:
    compiler:
        workspace_dir: .mega-steroids